package basicsofprogramming.operators.relationaloperators;

// Relational operators: For comparison purposes, x > y ,  x < y, x <= y
// In Java, Relational operators return boolean

// `==` => checks for equality
// boolean b = x == y

// `!=` ==> checks for non-equality
// boolean b = x != y

public class RelationalOperators {
    public static void main(String[] args) {
        boolean bool = 5 > 4;
        System.out.println(bool);
    }
}
